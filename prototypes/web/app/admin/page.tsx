'use client'

import { useState } from 'react'
import { Activity, ArrowUpRight, Check, ChevronRight, CircleAlert, MoreHorizontal, Store, TrendingUp, UserCheck, Users, X } from 'lucide-react'
import { AppFrame, DemoNotice, Money, SectionHeading, StatCard, StatusPill } from '@/components/app-shell'
import { mockAdminMetrics, mockStores } from '@/lib/mock-data'

export default function AdminPage() {
  const [view, setView] = useState<'overview' | 'usuarios' | 'lojas'>('overview')
  const [pending, setPending] = useState(true)
  return <AppFrame profile="admin"><div className="mx-auto max-w-6xl"><DemoNotice>Central administrativa do <strong>AçaíConecta</strong>. Explore os indicadores e simule ações de moderação.</DemoNotice>{view === 'overview' && <><SectionHeading eyebrow="Visão geral" title="A operação em um só lugar" description="Acompanhe a saúde do marketplace e mantenha a comunidade crescendo." action={<span className="inline-flex items-center gap-2 rounded-full bg-secondary/15 px-3 py-2 text-xs font-bold text-secondary"><Activity className="size-3.5" /> Sistema saudável</span>} /><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><StatCard label="Pedidos no mês" value="1.847" detail="+12,4% no período" icon={TrendingUp} tone="primary" /><StatCard label="Receita movimentada" value="R$ 45,2 mil" detail="Comissão média de 8%" icon={ArrowUpRight} tone="secondary" /><StatCard label="Clientes ativos" value="542" detail="86 novos este mês" icon={Users} tone="accent" /><StatCard label="Batedeiras ativas" value="03" detail="1 aguardando aprovação" icon={Store} tone="secondary" /></div><div className="mt-8 grid gap-6 lg:grid-cols-[1.2fr_0.8fr]"><section className="rounded-3xl border border-border bg-card p-5 sm:p-6"><div className="mb-6 flex items-center justify-between"><div><h2 className="text-lg font-black">Movimento da plataforma</h2><p className="mt-1 text-xs text-muted-foreground">Pedidos concluídos nos últimos 7 dias</p></div><button className="rounded-xl border border-border px-3 py-2 text-xs font-bold">Esta semana</button></div><div className="flex h-56 items-end gap-3 px-2">{[42, 56, 48, 72, 65, 88, 78].map((value, index) => <div key={index} className="flex flex-1 flex-col items-center gap-2"><div className="w-full rounded-t-xl bg-primary/80 transition hover:bg-primary" style={{ height: `${value}%` }} /><span className="text-[11px] text-muted-foreground">{['Seg', 'Ter', 'Qua', 'Qui', 'Sex', 'Sáb', 'Dom'][index]}</span></div>)}</div></section><section className="rounded-3xl border border-accent/20 bg-accent/10 p-6"><div className="flex items-start justify-between"><span className="grid size-10 place-items-center rounded-xl bg-accent/20 text-accent-foreground"><CircleAlert className="size-5" /></span><span className="rounded-full bg-background/70 px-2.5 py-1 text-[11px] font-bold">Ação necessária</span></div><h2 className="mt-6 text-xl font-black">Uma batedeira aguarda revisão</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Açaí da Ilha enviou documentos para entrar na rede.</p><button onClick={() => setView('lojas')} className="mt-5 inline-flex items-center gap-2 rounded-full bg-foreground px-4 py-2.5 text-xs font-bold text-background">Revisar cadastro <ChevronRight className="size-3.5" /></button></section></div><section className="mt-8 rounded-3xl border border-border bg-card p-5 sm:p-6"><div className="mb-5 flex items-center justify-between"><div><h2 className="text-lg font-black">Atividade recente</h2><p className="mt-1 text-xs text-muted-foreground">Últimas atualizações da comunidade</p></div><button onClick={() => setView('usuarios')} className="text-xs font-bold text-primary">Ver usuários</button></div><div className="flex flex-col divide-y divide-border">{[['João Silva', 'concluiu o pedido #AC-1040', 'há 2 min', 'bg-secondary/15 text-secondary'], ['Maria Santos', 'atualizou o cardápio da Açaí Amazônia', 'há 18 min', 'bg-primary/10 text-primary'], ['Ana Costa', 'criou uma nova conta de cliente', 'há 32 min', 'bg-accent/20 text-accent-foreground']].map(([name, action, time, tone]) => <div key={name} className="flex items-center gap-3 py-4"><span className={`grid size-9 place-items-center rounded-full text-xs font-bold ${tone}`}>{name.split(' ').map((part) => part[0]).join('')}</span><p className="flex-1 text-sm"><strong>{name}</strong> {action}<span className="block mt-1 text-xs text-muted-foreground">{time}</span></p><MoreHorizontal className="size-4 text-muted-foreground" /></div>)}</div></section></>}{view === 'usuarios' && <UsersView onBack={() => setView('overview')} />}{view === 'lojas' && <StoresView pending={pending} setPending={setPending} onBack={() => setView('overview')} />}</div></AppFrame>
}

function UsersView({ onBack }: { onBack: () => void }) { const users = [['João da Silva', 'joao@email.com', 'Cliente', 'Ativo'], ['Maria Santos', 'maria@acaia.com', 'Operador', 'Ativo'], ['Carlos Oliveira', 'carlos@puroacai.com', 'Operador', 'Ativo'], ['Ana Costa', 'ana@email.com', 'Cliente', 'Ativo'], ['Pedro Lima', 'pedro@email.com', 'Cliente', 'Pendente']]; return <><SectionHeading eyebrow="Comunidade" title="Usuários" description="Clientes e operadores cadastrados na plataforma." action={<button onClick={onBack} className="rounded-full border border-border px-4 py-2.5 text-sm font-bold">Voltar ao resumo</button>} /><div className="overflow-x-auto rounded-3xl border border-border bg-card"><table className="w-full min-w-[640px] text-left text-sm"><thead className="border-b border-border bg-muted/50 text-xs uppercase tracking-wider text-muted-foreground"><tr><th className="px-5 py-4 font-bold">Pessoa</th><th className="px-5 py-4 font-bold">Perfil</th><th className="px-5 py-4 font-bold">Status</th><th className="px-5 py-4" /></tr></thead><tbody className="divide-y divide-border">{users.map(([name, email, role, status]) => <tr key={email}><td className="px-5 py-4"><div className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-full bg-primary/10 text-xs font-bold text-primary">{name.split(' ').map((part) => part[0]).join('')}</span><div><p className="font-bold">{name}</p><p className="text-xs text-muted-foreground">{email}</p></div></div></td><td className="px-5 py-4 text-muted-foreground">{role}</td><td className="px-5 py-4"><StatusPill status={status === 'Ativo' ? 'open' : 'pending'} label={status} /></td><td className="px-5 py-4 text-right"><button aria-label={`Mais opções para ${name}`}><MoreHorizontal className="size-4 text-muted-foreground" /></button></td></tr>)}</tbody></table></div></> }

function StoresView({ pending, setPending, onBack }: { pending: boolean; setPending: (value: boolean) => void; onBack: () => void }) { return <><SectionHeading eyebrow="Rede de parceiros" title="Batedeiras" description="Revise cadastros, acompanhe status e cuide da qualidade da rede." action={<button onClick={onBack} className="rounded-full border border-border px-4 py-2.5 text-sm font-bold">Voltar ao resumo</button>} /><div className="mb-5 rounded-2xl border border-accent/25 bg-accent/10 p-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-center"><span className="grid size-10 place-items-center rounded-xl bg-accent/20 text-accent-foreground"><CircleAlert className="size-5" /></span><div className="flex-1"><p className="font-bold">Cadastro aguardando aprovação</p><p className="mt-1 text-sm text-muted-foreground">Açaí da Ilha · enviado há 2 horas</p></div>{pending ? <div className="flex gap-2"><button onClick={() => setPending(false)} className="inline-flex items-center gap-1.5 rounded-xl bg-secondary px-3 py-2 text-xs font-bold text-secondary-foreground"><Check className="size-3.5" /> Aprovar</button><button onClick={() => setPending(false)} className="inline-flex items-center gap-1.5 rounded-xl border border-border px-3 py-2 text-xs font-bold"><X className="size-3.5" /> Recusar</button></div> : <StatusPill status="open" label="Revisado" />}</div></div><div className="grid gap-4 sm:grid-cols-2">{mockStores.map((store) => <div key={store.id} className="flex gap-4 rounded-2xl border border-border bg-card p-4"><img src={store.image} alt="" className="size-20 rounded-xl object-cover" /><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-2"><div><h3 className="font-black">{store.name}</h3><p className="mt-1 text-xs text-muted-foreground">{store.owner}</p></div><StatusPill status={store.isOpen ? 'open' : 'closed'} /></div><div className="mt-4 flex items-center justify-between text-xs text-muted-foreground"><span>{store.rating} de avaliação</span><button className="font-bold text-primary">Ver detalhes <ChevronRight className="inline size-3" /></button></div></div></div>)}</div></> }
